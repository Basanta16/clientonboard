server:
  port: 5555
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/masterdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update